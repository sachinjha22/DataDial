<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.DataDial" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <!-- Shown while the app cold-starts. postSplashScreenTheme swaps back to Theme.DataDial
         once installSplashScreen() in MainActivity releases the splash. -->
    <style name="Theme.DataDial.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.DataDial</item>
    </style>
</resources>